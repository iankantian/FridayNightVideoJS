$( document ).ready( function() {
    console.log( 'document ready!' );
    //var fridayPlayer = videojs('my-video');
    //console.log( 'fridayPlayer looks like: ', fridayPlayer );
});

//dude!

$( document ).load( function() {
    console.log( "document loaded!" ); // doesn't fire when using videojs... interesting.
});